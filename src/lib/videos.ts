// src/lib/videos.ts
import { db } from "@/db";
import { videos } from "@/db/schema";

// export async function getVideos() {
//   return await db.query.videos.findMany({
//     // Add any conditions or ordering
//     orderBy: (videos, { description }) => [description(videos.createdAt)],
//   });
// }
